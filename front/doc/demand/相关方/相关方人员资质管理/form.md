证书上传：文件上传组件，必填，支持格式：JPG、PNG、PDF，单个文件≤10MB，上传后自动调用算法识别接口提取关键信息
●
姓名：文本输入框，通过算法自动识别填充，支持手动修改，必填，最大长度20字符
●
证件名称：文本输入框，通过算法自动识别填充，支持手动修改，必填，最大长度100字符
●
证书有效期起：日期选择器，通过算法自动识别填充，支持手动修改，必填，不能超过当前日期
●
选择人员：下拉选择框，必填，选项来源为系统中已建档的相关方人员列表，支持按姓名搜索
●
证书名称：文本输入框，必填，最大长度100字符，支持中英文及数字
●
证书类型：下拉选择框，必填，选项包括：特种作业证、职业资格证、安全培训证、专业技能证等
●
证书编号：文本输入框，必填，支持数字和字母，最大长度50字符，系统内需校验唯一性
●
证书有效期止：日期选择器，必填，必须晚于证书有效期起，支持算法自动识别填充
●
证书描述：多行文本输入框，非必填，最大长度500字符，支持换行和标点
●
保存按钮：操作按钮，提交前校验所有必填项及业务规则，校验不通过时提示具体错误信息
●
取消按钮：操作按钮，检测表单内容变更，如有修改需用户确认后放弃保存